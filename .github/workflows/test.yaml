name: "🧪 Test"

on:
  pull_request:
  push:
    branches:
      - "main"
      - "releases"

jobs:
  test:
    name: "${{ matrix.name }}"

    strategy:
      matrix:
        include:
          - name: "Windows"
            runner: "windows-latest"
            cpythons:
              - "3.9"
              - "3.10"
              - "3.11"
              - "3.12"
              - "3.13"
            tox-post-environments:
              - "coverage-report-ci"
              - "mypy"
            cache-paths:
              - ".mypy_cache/"
            cache-key-hash-files:
              - "pyproject.toml"
              - "requirements/*/*.txt"

    uses: "kurtmckee/github-workflows/.github/workflows/tox.yaml@f85528d9eb4c6af71bf17c82ab94c9404145b064" # v1.6
    with:
      config: "${{ toJSON(matrix) }}"
